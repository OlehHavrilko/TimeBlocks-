# Project-wide Gradle settings.

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
android.useAndroidX=true

# Enables namespacing of each library's R class so that its R class includes only the
android.nonTransitiveRClass=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables Jetifier for libraries that don't support AndroidX
android.enableJetifier=true

# Enables configuration caching (disabled due to transform issues)
# org.gradle.unsafe.configuration-cache=true
org.gradle.configuration-cache=false

# Enables build cache
org.gradle.caching=true

# R8 optimizations
android.enableR8.fullMode=true

# Kotlin JVM target validation (relaxed to avoid conflicts)
kotlin.jvm.target.validation.mode=warning

# Set Java home to use Java 17 for Gradle daemon
# org.gradle.java.home=C:/Program Files/Java/jdk-17
